// Group repository — group creation, membership queries, and group-level item lookups
import { prisma } from '@/lib/prisma'

export const groupsRepo = {
  // Sprint 2: create, findById, addMember, findMembersByGroup
} as const

export type GroupsRepo = typeof groupsRepo
